package management

var JwtSecret []byte

//
//func init() {
//	JwtSecret = []byte(os.Getenv("JWT_SECRET"))
//	if string(JwtSecret) == "" {
//		log.Fatal("Jwt secret cannot be null")
//	} else {
//		log.Println("JWT Secret: " + string(JwtSecret))
//	}
//}

func CreateTokenFromFields() {

}
